{
  "name": "my-eleventy-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @11ty/eleventy --input=src --output=build" // Adjust if your setup is different
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0", // Or whatever version you're using
    "metalsmith": "^2.3.0", 
    "metalsmith-layouts": "^1.11.1",
    "metalsmith-markdownit": "^1.1.2",
    "metalsmith-drafts": "^1.0.1", 
    "metalsmith-assets": "^0.1.3"
  }
}
